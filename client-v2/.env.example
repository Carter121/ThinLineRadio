#* All values are optional and baked in at build time (vite envPrefix PUBLIC_).
#* Leave PUBLIC_TLR_URL unset when the Go server serves this UI (same-origin).
PUBLIC_TLR_URL=""

#* MQTT dashboard tab (trunk-recorder-mqtt broker over websockets)
PUBLIC_MQTT_SERVER=""
PUBLIC_MQTT_WS_PORT=""
PUBLIC_MQTT_SECURE="false"
PUBLIC_MQTT_USERNAME=""
PUBLIC_MQTT_PASSWORD=""
PUBLIC_TOPIC="tr/feeds"
PUBLIC_UNIT_TOPIC="tr/units"
